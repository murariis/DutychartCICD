server {
    listen 80;

    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    # Handle React Router paths
    location / {
        try_files $uri /index.html;
    }

    # Optional: cache static assets
    location ~* \.(?:ico|css|js|gif|jpe?g|png|woff2?|eot|ttf|svg)$ {
        expires 1y;
        add_header Cache-Control "public";
    }
    
    # Proxy API to backend container to avoid CORS
    location ^~ /api/ {
        proxy_pass http://dcms-backend:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_set_header Authorization $http_authorization;
        proxy_redirect off;
        proxy_buffering off;
    }

    # Proxy Swagger/Redoc
    location ^~ /swagger/ {
        proxy_pass http://dcms-backend:8000/swagger/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
    }

    location ^~ /redoc/ {
        proxy_pass http://dcms-backend:8000/redoc/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
    }

    # Serve media files directly from mapped volume
    location ^~ /media/ {
        alias /usr/share/nginx/html/media/;
        try_files $uri $uri/ =404;
    }

    # Serve Mobile App APKs directly
    location ^~ /mobileApp/ {
        alias /usr/share/nginx/html/mobileApp/;
        try_files $uri =404;
        add_header Content-Type application/vnd.android.package-archive;
        add_header Content-Disposition "attachment";
    }

    # Serve static files directly from mapped volume
    location ^~ /static/ {
        alias /usr/share/nginx/html/static/;
        try_files $uri $uri/ =404;
    }

    # Optional: logs
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;
}
